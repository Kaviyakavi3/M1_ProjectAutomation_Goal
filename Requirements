We are providing enough security to satisy the user needs.The user has to enter the password to unlock the vehicle if correct vehicle gets started else OTP is send to an owner's mobile number.
Tools-Arduino IDE
